#include <stdio.h>
#include "main.h"
#include <stdlib.c>

int main(int argc, char *argv[])
{
		int calc = 0;

		if (argc == 3)
		{		
				 calc = atoi(argv[1] * atoi(argv[2]);
				printf("%d\n", c);
		}
		else
		{
				printf("Error\n");
				return(1);
		}
		return (0);
}

