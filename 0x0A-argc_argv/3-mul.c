#include <stdio.h>
#include "main.h"
#include <stdlib.c>

int main(int argc, char *argv[])
{
		int a
		int b

		if argc == 3
		{		
				int c = a*b
				printf("%d\n", c);
		}
		else
		{
				printf("Error\n");
				return(1);
		}
		return (0);
}

